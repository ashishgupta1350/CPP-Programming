#include<iostream>
using namespace std;

int main()
{
    int sum,pnum,N,i,j;

    cin<<N;
    for(i=1;i<=n;i++)
    {

        for(j=1;j<=100;j++)

        {
            sum=sum+(pnum%10);

            pnum/=10;
            if((num/10)==0)
                break;
            else
                continue;//see this for error

    }

    if (s%2==1)
    {
        if (s%3==0)
        {
            cout<<"Yes";
        }
        else
            cout<<"No";


    }

      if (s%2==0)
    {
        if (s%4==0)
        {
            cout<<"Yes";
        }
        else
            cout<<"No";


    }

    return 0;

}
